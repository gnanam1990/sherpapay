{
  "name": "@sherpapay/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "tsx watch src/worker.ts",
    "start": "node dist/worker.js",
    "lint": "eslint src/"
  },
  "dependencies": {
    "@sherpapay/core": "workspace:*",
    "@sherpapay/memory": "workspace:*",
    "@sherpapay/celo": "workspace:*",
    "pg": "^8.12.0",
    "node-cron": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/pg": "^8.11.0",
    "@types/node-cron": "^3.0.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
