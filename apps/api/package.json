{
  "name": "@sherpapay/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "lint": "eslint src/"
  },
  "dependencies": {
    "@sherpapay/core": "workspace:*",
    "@sherpapay/parser": "workspace:*",
    "@sherpapay/safety": "workspace:*",
    "@sherpapay/scheduler": "workspace:*",
    "@sherpapay/memory": "workspace:*",
    "@sherpapay/identity": "workspace:*",
    "fastify": "^5.0.0",
    "@fastify/cors": "^10.0.0",
    "@fastify/rate-limit": "^10.0.0",
    "zod": "^3.23.0",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "@types/pg": "^8.11.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
